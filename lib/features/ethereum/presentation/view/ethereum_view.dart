import 'package:flutter/material.dart';

class EthereumView extends StatelessWidget {
  const EthereumView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
